#include <stdio.h>

int main()
{ 
    //to check if number is even or odd
    int num1;
    printf("Enter value: ");
    scanf("%d", &num1);
    
    if (num1 % 2 == 0) {
        printf("The number is even");
    } else{
        printf("The number is odd");
    }
    return 0;
}
